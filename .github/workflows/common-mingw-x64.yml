name: common artifact - Windows x64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar.yml
    with:
      host: MingwX64
      runner: windows-2025

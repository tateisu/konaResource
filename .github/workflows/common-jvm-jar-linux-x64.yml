name: common-jvm jar - Linux x64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar.yml
    with:
      host: LinuxX64
      runner: ubuntu-24.04

name: common-jvm jar - macOS x64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar-host.yml
    with:
      host: MacosX64
      runner: macos-15-intel
      expected_targets: "MacosX64 MacosArm64"
      expected_resources: "macos-universal"

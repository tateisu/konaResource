name: common-jvm jar - Windows x64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar-host.yml
    with:
      host: WindowsX64
      runner: windows-2025
      expected_targets: "WindowsX64"
      expected_resources: "windows-x86_64"

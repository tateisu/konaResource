name: common-jvm jar - macOS arm64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar-host.yml
    with:
      host: MacosArm64
      runner: macos-15
      expected_targets: "MacosX64 MacosArm64"
      expected_resources: "macos-universal"

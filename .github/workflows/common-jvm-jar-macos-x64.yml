name: common-jvm jar - macOS x64

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common-jvm-jar.yml
    with:
      host: MacosX64
      runner: macos-15-intel
